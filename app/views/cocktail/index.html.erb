<h1>All Cocktails</h1>

<ul>
  <% @coacktails.each do |cocktail| %>
    <li><%= link_to(cocktail.name, cocktail_path(cocktail)) %></li>
  <% end %>
</ul>

<p><%= link_to 'Create a new cocktail', new_cocktail_path %></p>